const GroupList = ["<GROUPLIST>"] // test
const MiraiConfig = {
  url: "http://mirai:8080",
  key: "<KEY>",
}
const BotAccount = {
  id: "<ACCOUNT_ID>",
}

export { GroupList, MiraiConfig, BotAccount }
